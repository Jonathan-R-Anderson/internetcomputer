module mstd.file;

import core.stdc.stdio : fopen, fclose, fread, fwrite;
import core.stdc.string : strlen;
public import core.sys.posix.sys.stat;
public import core.sys.posix.unistd;
/// ditto
struct DirEntry { string name; }

module mstd.string;

import core.stdc.string : strlen, memcpy, memcmp, strcmp;

extern (C) int strcasecmp(const char *s1, const char *s2)
{
    while (*s1 != 0 && *s2 != 0)
    {
        char c1 = *s1;
        char c2 = *s2;
        if (c1 >= 'A' && c1 <= 'Z') c1 += 'a' - 'A';
        if (c2 >= 'A' && c2 <= 'Z') c2 += 'a' - 'A';
        if (c1 != c2) return c1 - c2;
        s1++;
        s2++;
    }
    return *s1 - *s2;
}

const(char)* toStringz(string s)
{
}

module ls;

import mstd.stdio;
import mstd.file;
import mstd.string;
import core.stdc.stdio : sprintf;

int ls(string[] args)
{
    // verbose implementation using sprintf
    bool all = false;
    bool longFmt = false;
    size_t idx = 1;
    while (idx < args.length)
    {
        if (args[idx] == "-a")
        {
            all = true;
            idx++;
        }
        else if (args[idx] == "-l")
        {
            longFmt = true;
            idx++;
        }
        else
        {
            break;
        }
    }

    if (idx >= args.length)
    {
        // Handle the case where no files or directories are provided
        return 0;
    }

    for (; idx < args.length; idx++)
    {
        string arg = args[idx];
        if (arg == "--")
        {
            idx++;
            break;
        }
        else if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {
            // Handle the case where "-" is used as a filename
            arg = "-";
        }

        if (arg == "--")
        {
            idx++;
            break;
        }

        if (arg == "-")
        {